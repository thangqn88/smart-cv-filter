@{
    ViewData["Title"] = "Edit Role";
    var roleId = ViewData["RoleId"];
}

<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
    <h1 class="h2">Edit Role</h1>
    <div class="btn-toolbar mb-2 mb-md-0">
        <a href="@Url.Action("Index")" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left"></i>
            Back to List
        </a>
    </div>
</div>

<div class="row">
    <div class="col-12">
        <div class="card shadow">
            <div class="card-body text-center py-5">
                <i class="bi bi-shield-check display-1 text-muted"></i>
                <h3 class="text-muted mt-3">Edit Role</h3>
                <p class="text-muted">Role editing functionality will be implemented in a future update.</p>
                <p class="text-muted">Role ID: @roleId</p>
                <a href="@Url.Action("Index")" class="btn btn-primary">
                    <i class="bi bi-arrow-left"></i>
                    Back to Role Management
                </a>
            </div>
        </div>
    </div>
</div>
